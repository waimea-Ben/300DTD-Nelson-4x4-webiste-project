{% extends 'pages/base.jinja' %}

{# -------------------------------------------------------------
   Page Title Block — sets the browser/tab title
------------------------------------------------------------- #}
{% block title %}
    Admin
{% endblock %}


{# -------------------------------------------------------------
   Main Content Block — club settings management
------------------------------------------------------------- #}
{% block content %}
<hr class="header-line">

{# Tab navigation for admin sections #}
<div class="tab-bar">
    <a href="/admin/settings" class="tab active">settings</a>
    <a href="/admin/trips" class="tab">trips</a>
    <a href="/admin/members" class="tab">Members</a>
</div>

{# Club information settings form #}
<article id="admin-settings">
    <header>
        <h2>Club Information</h2>
        <p>Update basic club details</p>
    </header>

    <form method="post" action="/admin/settings">
        <label>Site Name:</label>
        <input type="text" name="site_name" value="{{ settings.site_name }}">

        <label>Join Price:</label>
        <input type="number" step="0.01" name="join_price" value="{{ settings.join_price }}" min="0.00">

        <label>Contact Email:</label>
        <input type="email" name="contact_email" value="{{ settings.contact_email }}">

        {# Submit button #}
        <button type="submit">Save</button>
    </form>
</article>

{% endblock %}
